# 코드 컨벤션

직접 작성한 C++ 코드와 GLSL 셰이더는 줄바꿈을 충분히 사용하는 형식으로 작성합니다.
포맷 설정은 루트의 `.clang-format`에 있습니다. 적용 도구는 clang-format 19 이상을 기준으로 합니다.

- 들여쓰기는 공백 4칸을 사용합니다.
- 함수, 클래스, 구조체, 조건문, 반복문의 여는 중괄호는 다음 줄에 둡니다(Allman).
- 한 줄에 여러 실행문을 붙여 쓰지 않습니다.
- 한 문장짜리 조건문과 반복문에도 중괄호를 사용합니다.
- 짧은 함수, getter, 람다, 빈 함수도 여러 줄로 작성합니다.
- 함수와 타입 정의 사이, 접근 지정자 앞뒤에는 빈 줄을 둡니다.
- 한 줄은 100자를 기준으로 하며, 긴 인수 목록은 인수별로 줄을 나눕니다.
- 생성자 초기화 목록이 여러 항목이면 항목별로 줄을 나눕니다.
- 포인터와 참조는 `Renderer*`, `const Device&`처럼 타입에 붙입니다.
- include 순서는 유지합니다. C++ 소스의 `stdafx.h`는 첫 번째 include로 둡니다.
- 파일 줄바꿈은 Windows 프로젝트에 맞게 CRLF를 사용합니다.
- `SimpleGame/Dependencies`의 외부 코드는 포맷 대상에서 제외합니다.

```cpp
bool Prototype::Save()
{
    if (!saveAllowed_)
    {
        Notify("SAVE DISABLED");
        return false;
    }

    return world_.Save(savePath_, player_);
}
```

Visual Studio의 clang-format 지원에서 이 설정을 사용하거나, clang-format에 파일을 명시하여 적용합니다.
빌드 및 실행 확인은 별도 요청이 없는 한 사용자가 직접 진행합니다.
